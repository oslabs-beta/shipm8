import { createSlice } from '@reduxjs/toolkit';

const GoogleCloud = createSlice({
  name: 'GoogleCloud',
  initialState: {

  },
  reducers: {

  }
});

export const { } = GoogleCloud.actions;

export default GoogleCloud.reducer;
